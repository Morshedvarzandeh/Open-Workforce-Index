# OWI Office graphics pack v2 — rights notice

The repository's Apache-2.0 license applies to source code and original
metadata only where the repository owner has the necessary rights. It must not
be read as a declaration that every bitmap in this development graphics pack
has completed a redistribution review.

Every bitmap currently registered by `manifest.json` has `rights.status` set
to `pending`. `SOURCES.json` records the known generation or import trail, but
does not grant a license. Do not publish, redistribute, or relicense this pack
until a maintainer records the owner, evidence, review date, reviewer, and a
valid license expression, then passes `python3 validate.py --release`.

Unknown or pending evidence must remain unknown or pending. A file copy,
format conversion, checksum, or technical validation does not create rights.
